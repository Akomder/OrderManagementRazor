@page
@model OrderManagementRazor.Pages.Reports.ProductCustomersModel
@{
    ViewData["Title"] = "Product Customers Report";
}

<div class="container mt-4">
    <h2 class="mb-4">Items Purchased by Customers Report</h2>

    <div class="card mb-4">
        <div class="card-header">
            <h5>Filter Options</h5>
        </div>
        <div class="card-body">
            <form method="get">
                <div class="row">
                    <div class="col-md-4 mb-3">
                        <label asp-for="ProductId" class="form-label">Product</label>
                        <select asp-for="ProductId" asp-items="Model.Products" class="form-select">
                            <option value="">-- All Products --</option>
                        </select>
                    </div>

                    <div class="col-md-3 mb-3">
                        <label asp-for="StartDate" class="form-label"></label>
                        <input asp-for="StartDate" class="form-control" type="date" />
                    </div>

                    <div class="col-md-3 mb-3">
                        <label asp-for="EndDate" class="form-label"></label>
                        <input asp-for="EndDate" class="form-control" type="date" />
                    </div>

                    <div class="col-md-2 mb-3 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="bi bi-search"></i> Filter
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    @if (Model.ProductCustomers.Any())
    {
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Product Purchase Details by Customers</h5>
                <button onclick="window.print()" class="btn btn-sm btn-success no-print">
                    <i class="bi bi-printer"></i> Print Report
                </button>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>Product Name</th>
                                <th>Category</th>
                                <th>Customer Name</th>
                                <th>Email</th>
                                <th class="text-center">Total Qty</th>
                                <th class="text-end">Total Spent</th>
                                <th class="text-center">Orders</th>
                                <th>First Purchase</th>
                                <th>Last Purchase</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.ProductCustomers)
                            {
                                <tr>
                                    <td><strong>@item.ProductName</strong></td>
                                    <td><span class="badge bg-secondary">@item.Category</span></td>
                                    <td>@item.CustomerName</td>
                                    <td>@item.CustomerEmail</td>
                                    <td class="text-center">@item.TotalQuantity</td>
                                    <td class="text-end"><strong>@item.TotalSpent.ToString("C")</strong></td>
                                    <td class="text-center">@item.OrderCount</td>
                                    <td>@item.FirstPurchase.ToString("MM/dd/yyyy")</td>
                                    <td>@item.LastPurchase.ToString("MM/dd/yyyy")</td>
                                </tr>
                            }
                        </tbody>
                        <tfoot class="table-light">
                            <tr>
                                <th colspan="4" class="text-end">Total:</th>
                                <th class="text-center">@Model.ProductCustomers.Sum(p => p.TotalQuantity)</th>
                                <th class="text-end">@Model.ProductCustomers.Sum(p => p.TotalSpent).ToString("C")</th>
                                <th class="text-center">@Model.ProductCustomers.Sum(p => p.OrderCount)</th>
                                <th colspan="2"></th>
                            </tr>
                        </tfoot>
                    </table>
                </div>

                <div class="mt-4">
                    <h5>Summary Statistics</h5>
                    <div class="row">
                        <div class="col-md-3">
                            <div class="card bg-primary text-white">
                                <div class="card-body">
                                    <h6>Total Products</h6>
                                    <h3>@Model.ProductCustomers.Select(p => p.ProductId).Distinct().Count()</h3>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-success text-white">
                                <div class="card-body">
                                    <h6>Total Customers</h6>
                                    <h3>@Model.ProductCustomers.Select(p => p.CustomerId).Distinct().Count()</h3>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-info text-white">
                                <div class="card-body">
                                    <h6>Total Units Sold</h6>
                                    <h3>@Model.ProductCustomers.Sum(p => p.TotalQuantity)</h3>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-warning text-white">
                                <div class="card-body">
                                    <h6>Total Revenue</h6>
                                    <h3>@Model.ProductCustomers.Sum(p => p.TotalSpent).ToString("C")</h3>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <i class="bi bi-info-circle"></i> No data found for the selected filters.
        </div>
    }
</div>

<style>
    @@media print {
        .no-print {
            display: none !important;
        }
        .container {
            max-width: 100% !important;
        }
    }
</style>
